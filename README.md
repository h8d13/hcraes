# hcraes

Tiny zero-dep metasearch.

## Sources

All CORS-enabled JSON/xml APIs, no auth needed. Other sources are deemed "unfit".

## Run locally

Any static server:

```sh
python3 -m http.server 8000
# open http://localhost:8000
```

Or `npx serve`, VS Code Live Server, etc.
